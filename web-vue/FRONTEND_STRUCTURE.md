# 前端目录结构说明

## 📁 优化后的目录结构

```
web-vue/
├── src/
│   ├── api/                      # API 接口层
│   │   ├── agent.ts             # 智能体 API
│   │   ├── plugin.ts            # 插件 API
│   │   └── user.ts              # 用户 API
│   │
│   ├── components/               # 组件库 ✨
│   │   ├── common/              # 通用组件
│   │   │   ├── Card.vue         # 卡片组件
│   │   │   ├── PageHeader.vue   # 页面头部
│   │   │   └── GridContainer.vue # 网格容器
│   │   │
│   │   ├── agent/               # 智能体业务组件
│   │   │   ├── AgentCard.vue    # 智能体卡片
│   │   │   └── AgentFormDialog.vue # 智能体表单弹窗
│   │   │
│   │   └── plugin/              # 插件业务组件
│   │       └── PluginCard.vue   # 插件卡片
│   │
│   ├── layouts/                  # 布局组件 ✨
│   │   └── MainLayout.vue       # 主布局（带侧边栏和顶栏）
│   │
│   ├── router/                   # 路由配置
│   │   └── index.ts             # 路由定义
│   │
│   ├── stores/                   # 状态管理
│   │   └── user.ts              # 用户状态
│   │
│   ├── styles/                   # 样式系统 ✨
│   │   ├── variables.css        # 设计变量（颜色、间距、字体等）
│   │   ├── base.css             # 基础样式重置
│   │   ├── utilities.css        # 原子化工具类
│   │   ├── components.css       # 通用组件样式
│   │   └── index.css            # 样式入口
│   │
│   ├── utils/                    # 工具函数
│   │   └── http.ts              # HTTP 请求封装
│   │
│   ├── views/                    # 页面视图 ✨
│   │   ├── agents/              # 智能体模块
│   │   │   ├── index.vue        # 智能体列表页
│   │   │   └── [id].vue         # 智能体详情页（待创建）
│   │   │
│   │   ├── plugins/             # 插件模块
│   │   │   ├── index.vue        # 插件列表页
│   │   │   └── [id].vue         # 插件详情页（待创建）
│   │   │
│   │   ├── auth/                # 认证模块
│   │   │   ├── Login.vue        # 登录页
│   │   │   └── Register.vue     # 注册页
│   │   │
│   │   └── console/             # 旧控制台页面（待清理）
│   │       ├── AgentDetail.vue  # 智能体详情（临时）
│   │       └── PluginDetail.vue # 插件详情（临时）
│   │
│   ├── App.vue                   # 根组件
│   ├── main.ts                   # 应用入口
│   └── vite-env.d.ts            # TypeScript 类型声明
│
├── public/                       # 公共静态文件
├── index.html                    # HTML 模板
├── package.json                  # 项目依赖
├── tsconfig.json                # TypeScript 配置
├── vite.config.ts               # Vite 配置
└── README.md                     # 项目说明
```

## 🎯 目录组织原则

### 1. **按功能模块划分**

- `views/` - 页面级组件，对应路由
  - 每个模块一个文件夹（agents, plugins, auth 等）
  - index.vue 为列表页，[id].vue 为详情页

### 2. **组件分层设计**

- `components/common/` - 通用 UI 组件，可跨项目复用
- `components/agent/` - 智能体业务组件
- `components/plugin/` - 插件业务组件
- 遵循从通用到特定的层次结构

### 3. **样式模块化**

- `styles/variables.css` - 设计令牌（Design Tokens）
- `styles/utilities.css` - 原子化 CSS 类
- `styles/components.css` - 组件级样式复用
- 优先使用 CSS 变量，避免硬编码

### 4. **清晰的命名约定**

- 页面文件：`index.vue` (列表), `[id].vue` (详情)
- 组件文件：大驼峰 `AgentCard.vue`
- 工具函数：小驼峰 `formatDate`
- 常量：大写下划线 `API_BASE_URL`

## 🎨 设计系统

### CSS 变量（Design Tokens）

```css
/* 颜色系统 */
--color-primary: #3b82f6; /* 主色 */
--color-success: #10b981; /* 成功 */
--color-warning: #f59e0b; /* 警告 */
--color-danger: #ef4444; /* 危险 */

/* 间距系统 */
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 32px;

/* 字体系统 */
--font-size-xs: 12px;
--font-size-sm: 14px;
--font-size-base: 16px;
--font-size-lg: 18px;
--font-size-xl: 20px;

/* 阴影系统 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
```

### 原子化工具类

```css
/* 布局 */
.flex {
  display: flex;
}
.flex-col {
  flex-direction: column;
}
.items-center {
  align-items: center;
}
.justify-between {
  justify-content: space-between;
}

/* 间距 */
.p-sm {
  padding: var(--spacing-sm);
}
.m-md {
  margin: var(--spacing-md);
}
.gap-md {
  gap: var(--spacing-md);
}

/* 文本 */
.text-sm {
  font-size: var(--font-size-sm);
}
.text-muted {
  color: var(--color-text-secondary);
}
.font-semibold {
  font-weight: 600;
}

/* 圆角和阴影 */
.rounded-lg {
  border-radius: var(--radius-lg);
}
.shadow-card {
  box-shadow: var(--shadow-md);
}
```

## 📦 组件使用示例

### 1. PageHeader（页面头部）

```vue
<PageHeader title="智能体管理" subtitle="创建和管理您的 AI 智能体">
  <template #actions>
    <el-button type="primary" @click="handleCreate">
      <el-icon><Plus /></el-icon>
      创建智能体
    </el-button>
  </template>
</PageHeader>
```

### 2. GridContainer（网格容器）

```vue
<GridContainer :columns="3" :gap="24">
  <Card
    v-for="item in items"
    :key="item.id"
    :title="item.name"
    :icon="item.icon"
  />
</GridContainer>
```

### 3. Card（卡片组件）

```vue
<Card
  title="GPT-4 助手"
  icon="🤖"
  :meta="[
    { label: '类型', value: 'AI 助手' },
    { label: '状态', value: '运行中', type: 'success' },
  ]"
  :tags="['GPT-4', '对话']"
  @click="handleClick"
>
  <template #menu>
    <el-dropdown-item>编辑</el-dropdown-item>
    <el-dropdown-item>删除</el-dropdown-item>
  </template>
  
  <template #footer>
    <div class="text-muted text-sm">
      更新于 2024-01-15
    </div>
  </template>
</Card>
```

## 🔧 技术栈

- **框架**: Vue 3.5 + TypeScript
- **构建工具**: Vite 6.0
- **UI 库**: Element Plus 2.8
- **状态管理**: Pinia 2.2
- **路由**: Vue Router 4.5
- **HTTP 客户端**: Axios
- **代码规范**: ESLint + Prettier

## 📝 开发规范

### 组件开发

1. 使用 `<script setup lang="ts">` 语法
2. 合理拆分组件，单一职责原则
3. Props 定义使用 TypeScript 接口
4. 事件命名使用 `on` 前缀（onUpdate, onClick）

### 样式规范

1. 优先使用 CSS 变量和工具类
2. 避免深层嵌套（最多 3 层）
3. 使用 scoped 样式防止污染
4. 响应式设计使用设计系统断点

### API 调用

1. 所有 API 封装在 `api/` 目录
2. 使用统一的错误处理
3. 接口定义 TypeScript 类型
4. 敏感操作添加 loading 和确认

## 🚀 下一步优化

- [ ] 移动详情页到新结构（views/agents/[id].vue）
- [ ] 清理旧的 console 目录文件
- [ ] 添加工作流管理模块
- [ ] 添加知识库管理模块
- [ ] 完善 TypeScript 类型定义
- [ ] 添加单元测试
- [ ] 添加组件文档和 Storybook

## 💡 为什么这样设计？

### 问题：原有结构的不足

- ❌ 组件和页面混在 `views/` 下
- ❌ 没有独立的 `components/` 目录
- ❌ 布局组件放在 `views/layout/`
- ❌ 命名带有 Console、Designer 等后缀
- ❌ 样式分散，缺少设计系统

### 解决方案：新结构的优势

- ✅ 清晰的组件分层（common → business → pages）
- ✅ 独立的 `layouts/` 和 `components/` 目录
- ✅ 按功能模块组织页面（agents/, plugins/）
- ✅ 简洁的命名（index.vue, [id].vue）
- ✅ 统一的设计系统和原子化 CSS

### 最佳实践参考

- Vue 官方风格指南
- Nuxt.js 目录结构
- Element Plus 组件设计
- Tailwind CSS 工具类思想

### 1. 设计变量 (variables.css)

- **颜色系统**: 主题色、功能色、中性色
- **间距系统**: 4px、8px、16px、24px、32px、48px
- **字体系统**: 字号、字重、行高
- **圆角系统**: 4px、8px、12px、16px、full
- **阴影系统**: sm、md、lg、xl
- **动画系统**: fast(150ms)、base(200ms)、slow(300ms)

### 2. 工具类 (utilities.css)

提供了大量原子化 CSS 类，类似 Tailwind CSS：

- **布局**: flex、grid、items-center、justify-between 等
- **间距**: m-4、p-6、gap-2 等
- **文本**: text-lg、font-bold、text-center 等
- **圆角**: rounded、rounded-lg、rounded-full 等
- **阴影**: shadow-sm、shadow-md、shadow-lg 等

### 3. 组件样式 (components.css)

- **页面容器**: page-container、page-header、page-title
- **卡片**: card、card-header、card-body、card-footer
- **网格**: grid、grid-cols-3、md:grid-cols-2
- **徽章**: badge、badge-primary、badge-success
- **骨架屏**: skeleton（加载动画）
- **空状态**: empty-state

## 🎯 优化重点

### 1. 现代化设计

- ✅ 使用 CSS 变量实现主题系统
- ✅ 采用现代渐变色和阴影效果
- ✅ 更大的圆角和间距，更舒适的视觉体验
- ✅ 精心设计的悬停和过渡动画
- ✅ 响应式设计支持

### 2. 代码规范

- ✅ 统一的设计变量，便于主题定制
- ✅ 原子化 CSS 类，提高开发效率
- ✅ 语义化的命名规范
- ✅ TypeScript 类型支持
- ✅ 组件化和模块化

### 3. 用户体验

- ✅ 流畅的页面过渡动画
- ✅ 卡片悬停效果和交互反馈
- ✅ 清晰的视觉层次
- ✅ 统一的品牌形象
- ✅ 更好的可读性和可访问性

## 🚀 使用建议

### 添加新页面

1. 在 `views/` 对应目录创建 Vue 文件
2. 使用设计变量和工具类
3. 在 `router/index.ts` 添加路由

### 创建组件

1. 在 `components/` 相应分类创建组件
2. 使用设计系统中的变量
3. 遵循命名规范

### 样式编写

```vue
<style scoped>
/* 优先使用 CSS 变量 */
.my-button {
  padding: var(--spacing-md);
  border-radius: var(--radius-lg);
  background: var(--color-primary);
  transition: all var(--transition-base) ease;
}

.my-button:hover {
  background: var(--color-primary-dark);
  box-shadow: var(--shadow-md);
}
</style>
```

### 工具类使用

```vue
<template>
  <div class="flex items-center justify-between p-4 rounded-lg shadow-md">
    <h2 class="text-2xl font-bold">标题</h2>
    <button class="px-4 py-2 rounded-full">按钮</button>
  </div>
</template>
```

## 📝 待优化项

- [ ] 添加 `components/` 公共组件
- [ ] 添加深色模式支持
- [ ] 优化移动端响应式
- [ ] 添加骨架屏加载状态
- [ ] 添加错误边界处理
- [ ] 完善 TypeScript 类型定义
- [ ] 添加单元测试
- [ ] 性能优化（代码分割、懒加载）

## 🎨 配色方案

### 主色调

- **Primary**: #6366f1 (靛蓝色) - 现代、专业、科技感
- **Success**: #10b981 (绿色) - 成功、完成
- **Warning**: #f59e0b (橙色) - 警告、注意
- **Error**: #ef4444 (红色) - 错误、危险

### 渐变色

- **智能体卡片**: 紫色渐变 (#6366f1 → #4f46e5)
- **插件卡片**: 粉色渐变 (#ec4899 → #db2777)
- **品牌标识**: 主色渐变

## 💡 最佳实践

1. **保持一致性**: 使用设计系统中的变量和组件
2. **语义化命名**: 使用有意义的类名和变量名
3. **组件化思维**: 拆分可复用的组件
4. **性能优先**: 避免不必要的重渲染
5. **可访问性**: 考虑键盘导航和屏幕阅读器
